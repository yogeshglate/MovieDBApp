import { StyleSheet } from 'react-native';
import { Colors } from '../../theme';

export const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: Colors.detailsBackground },
  detailsView: {
    alignItems: 'center',
    backgroundColor: Colors.detailsBackground,
  },
});
